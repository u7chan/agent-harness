# スキル命名規約

このリポジトリでは、スキル名・ディレクトリ名・`SKILL.md` の front matter にある
`name` を一致させる。

## GitHub 関連スキル（`gh-*`）

GitHub 関連スキルは、原則として次の形式を使う。

```text
gh-<verb>-<object>
```

- `<verb>` はスキルの主な操作を表す短い動詞にする。例: `create`、`inspect`、`handle`。
- `<object>` は操作対象を表す短い名詞句にする。例: `pr`、`ci`、`pr-feedback`。
- すべて小文字の kebab-case を使い、略語は既存の `pr`、`ci` のように広く通じるものだけを使う。
- 名前は一つの主責務を表す。複数の操作があっても、利用者が依頼するときの主目的を動詞に選ぶ。

この形式により、一覧では `gh-` でまとまり、名前だけで「何をするスキルか」を判別できる。

## 現在の判断

| スキル | 判断 | 理由 |
| --- | --- | --- |
| `gh-create-pr` | 維持 | PR を作成する主責務を表しており、規約に適合する。 |
| `gh-inspect-ci` | 維持 | CI を検査する主責務を表しており、規約に適合する。 |
| PR フィードバックを扱う新規スキル | `gh-handle-pr-feedback` を採用 | 取得・整理・明示承認後の返信を含む主責務を表す。 |

したがって、現存する `gh-*` スキルの改名は行わない。`gh-pr-feedback` は今後の実装で使わず、関連 Issue の要件を `gh-handle-pr-feedback` に更新してから実装する。

## 改名が必要になった場合

改名は互換性に影響するため、通常の機能追加とは分けて判断する。実施する場合は次を行う。

1. `rg` で旧名の参照、シンボリックリンク、起動条件、ドキュメントを洗い出す。
2. ディレクトリ名、front matter の `name`、参照箇所を同じ変更で更新する。
3. 利用者向けの移行手順と、必要なら旧名のサポート終了時期をリリースノートに記載する。
4. スキル検証と、旧名・新名を参照する導線の確認を行う。

スキル探索で重複登録を招くため、互換目的のディレクトリ・シンボリックリンクは、対応する探索環境で安全性を確認できない限り作成しない。
